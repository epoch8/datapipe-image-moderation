version: '3'

services:
  postgres:
    image: postgres:12
    container_name: test-moderation-step-database
    ports:
      - '5432:5432'
    environment:
      POSTGRES_NAME: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
